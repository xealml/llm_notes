# 一些基础的矩阵运算

## 1. 语言模型使用公式表示
设定输入的序列为
$x_1, x_2, .... x_T$

整个序列使用链式法则可以分解成
$P(x_1,x_2,...,x_T) =  \displaystyle \prod_{t=1}^T P(x_t|x_{<t})$

其中
$x_{<t} = (x_1, x_2, ..., x_{t-1})$

语言模型的训练目标,最大化似然
$\displaystyle \sum _{t=1} ^ T log P(x_t| x_{<t})$

对应可以等价成，最小化负对数似然
$L = - \displaystyle \sum _{t=1}^{T} log P(x_t|x_{<t})$ 

## 2. 位置编码

### 1.绝对位置编码（经典正弦位置编码）

Transformer 原始论文中的公式：

$$
PE(_{pos, 2i}) = sin(\frac {pos} {10000^{2i/d}} )
\\
PE(_{pos, 2i+1}) = cos(\frac {pos} {10000^{2i/d}})
$$

其中: 
- pos=位置，表示为输入时第几个token
- i=第pos个token对应的第i个维度
- d是embedding维度

### 2. 可学习的位置编码

直接给每个位置一个可学习的变量

$$
h_t^{(0)} = e_t + PE_t  
$$ 

其中$p_t$是第$t$个位置的位置编码;

### 3.RoPE

RoPE的核心是：把位置信息通过旋转操作，注入到Q和K中，从而让注意力依赖相对位置，对二维向量对$(x_{2i}, x_{2i+1})$旋转后：

$$
\begin{pmatrix}
x'_{2i} \\
x'_{2i+1}
\end{pmatrix}

= 

\begin{pmatrix}
\cos \theta_i(m) & -\sin \theta_i(m) \\
\sin \theta_i(m) & \cos \theta_i(m)
\end{pmatrix}
\begin{pmatrix}
x_{2i} \\
x_{2i+1}
\end{pmatrix}

$$

其中
$$
\theta_i(m) = m \cdot \omega_i
$$

- m表示位置
- $\omega_i$是不同维度对应的频率

RoPE 的好处是注意力分数中自然出现相对位置差 $m−n$，更适合长序列建模。

- 参考
    - Transformer升级之路：1、Sinusoidal位置编码追根溯源:https://www.spaces.ac.cn/archives/8231
    - Transformer升级之路：2、博采众长的旋转式位置编码:https://www.spaces.ac.cn/archives/8265
    - 让研究人员绞尽脑汁的Transformer位置编码：https://www.spaces.ac.cn/archives/8130

从头推一下rope的公式，先说rope的本质
> 对向量的每一对维度，按照输入的token的位置，旋转一个角度，对表征成向量的token，设为d维度，总计有d/2对可以进行旋转的参数；

对其中一组维度的参数 
$$
(x_{2i}, x_{2i+1})  \tag{1}
$$

$$
z_i=x_{2i}+jx_{2i+1} \tag{2}
$$
将其看成复数形式

> attention 

这里先解释一下为什么可以变成复数的形式

复指数 $e^{j\theta}$,它看起来像普通指数，但指数不是实数，而是复数。

这里：

- e 是自然常数，约等于 2.71828
- j 是虚数单位（在数学里常写 i，工程里常写 j）
- $\theta$ 是一个角度

#### 1. 最重要的公式
$$
e^{j\theta} = cos\theta+jsin\theta
$$
这句话非常重要。 它说明： 复指数其实就是一个“旋转”的表示法。

#### 2. 几何意义

在复平面，一个复数可以看成二维坐标
$$
a + jb \quad \leftrightarrow \quad (a,b)
$$
所以
$$
cos\theta+jsin\theta \quad \rightarrow \quad (cos\theta, sin\theta)
$$
这个点刚好在单位圆上，角度是 θ.
所以
$$
e^{j\theta}
$$
表示的是
>单位圆上，朝向角度 是 $\theta$ 的那个点

#### 3. 为什么叫指数
满足指数运算规律
$$
e^{j\alpha} e^{j\beta} = e^{j(\alpha+\beta)}
$$

几何上这意味着:
- 乘以 $e^{j\alpha}$ ：旋转$\alpha$
- 再乘以 $e^{j\beta}$: 再旋转$\beta$
- 总体效果就是旋转$\alpha + \beta $
#### 4. 在RoPE里的意思
RoPE 里写成

$$
z'=z \cdot e^{j\theta}
$$

意思是把z在复平面旋转$\theta$角度

若$z=x+jy$, 
则 $z'=(x+jy)(cos\theta+jsin\theta)$

展开得到
$$
z'= xcos\theta - y sin \theta + (x sin \theta + y cos \theta) i
$$

分离实部和虚部

$$
x'=x cos \theta - y sin \theta \tag{1}
$$

$$
y'= x sin \theta + y cos \theta \tag{2}
$$

写成矩阵的形式
$$
\begin{pmatrix}
x'\\
y'
\end{pmatrix}

= 

\begin{pmatrix}
\cos \theta & -\sin \theta \\
\sin \theta &  \cos \theta
\end{pmatrix}
\begin{pmatrix}
x \\
y
\end{pmatrix}

$$

所以，就等价于
$z'=ze^{j \theta}$




deepseek-v3实现：https://github.com/deepseek-ai/DeepSeek-V3/blob/main/inference/model.py#L378
```python

def precompute_freqs_cis(args: ModelArgs) -> torch.Tensor:
    """
    Precomputes frequency-based complex exponential values for rotary positional embeddings.

    Args:
        args (ModelArgs): Model arguments containing positional embedding parameters.

    Returns:
        torch.Tensor: Precomputed complex exponential values for positional embeddings.
    """
    dim = args.qk_rope_head_dim
    seqlen = args.max_seq_len
    beta_fast = args.beta_fast
    beta_slow = args.beta_slow
    base = args.rope_theta
    factor = args.rope_factor

    def find_correction_dim(num_rotations, dim, base, max_seq_len):
        """
        Computes the correction dimension for a given number of rotations in the rotary positional embedding.

        Args:
            num_rotations (float): Number of rotations to compute the correction for.
            dim (int): Dimensionality of the embedding space.
            base (float): Base value for the exponential computation.
            max_seq_len (int): Maximum sequence length.

        Returns:
            float: The correction dimension based on the input parameters.
        """
        return dim * math.log(max_seq_len / (num_rotations * 2 * math.pi)) / (2 * math.log(base))

    def find_correction_range(low_rot, high_rot, dim, base, max_seq_len):
        """
        Computes the range of correction dimensions for rotary positional embeddings.

        Args:
            low_rot (float): Lower bound for the number of rotations.
            high_rot (float): Upper bound for the number of rotations.
            dim (int): Dimensionality of the embedding space.
            base (float): Base value for the exponential computation.
            max_seq_len (int): Maximum sequence length.

        Returns:
            Tuple[int, int]: The range of correction dimensions (low, high), clamped to valid indices.
        """
        low = math.floor(find_correction_dim(low_rot, dim, base, max_seq_len))
        high = math.ceil(find_correction_dim(high_rot, dim, base, max_seq_len))
        return max(low, 0), min(high, dim-1)

    def linear_ramp_factor(min, max, dim):
        """
        Computes a linear ramp function used to smooth values between a minimum and maximum range.

        Args:
            min (float): Minimum value for the ramp function.
            max (float): Maximum value for the ramp function.
            dim (int): Dimensionality of the ramp tensor.

        Returns:
            torch.Tensor: A tensor of shape (dim,) with values linearly interpolated between 0 and 1,
                clamped to the range [0, 1].
        """
        if min == max:
            max += 0.001
        linear_func = (torch.arange(dim, dtype=torch.float32) - min) / (max - min)
        ramp_func = torch.clamp(linear_func, 0, 1)
        return ramp_func

    freqs = 1.0 / (base ** (torch.arange(0, dim, 2, dtype=torch.float32) / dim))
    if seqlen > args.original_seq_len:
        low, high = find_correction_range(beta_fast, beta_slow, dim, base, args.original_seq_len)
        smooth = 1 - linear_ramp_factor(low, high, dim // 2)
        freqs = freqs / factor * (1 - smooth) + freqs * smooth

    t = torch.arange(seqlen)
    freqs = torch.outer(t, freqs)
    freqs_cis = torch.polar(torch.ones_like(freqs), freqs)
    return freqs_cis


def apply_rotary_emb(x: torch.Tensor, freqs_cis: torch.Tensor) -> torch.Tensor:
    """
    Applies rotary positional embeddings to the input tensor.

    Args:
        x (torch.Tensor): Input tensor with positional embeddings to be applied.
        freqs_cis (torch.Tensor): Precomputed complex exponential values for positional embeddings.

    Returns:
        torch.Tensor: Tensor with rotary embeddings applied.
    """
    dtype = x.dtype
    x = torch.view_as_complex(x.float().view(*x.shape[:-1], -1, 2))
    freqs_cis = freqs_cis.view(1, x.size(1), 1, x.size(-1))
    y = torch.view_as_real(x * freqs_cis).flatten(3)
    return y.to(dtype)

freqs_cis = precompute_freqs_cis()
q_pe = apply_rotary_emb(q_pe, freqs_cis)

```
#### 5. 2d RoPE

一句话总结，把输入的图像patch对应的emb，比如为d维，拆分成两份，每份d/2维
然后按照横纵坐标分别对前后两份做RoPE即可。

## 3. Attention

### 3.1 输入层的表征为
$$X \in \mathbb{R} ^ {S \times d}$$

其中: 
- S: 序列长度
- d: 隐藏层维度

### 3.2 生成 Q、K、V
通过三个线性变化得到
$$
Q = XW_Q 
\\
K = XW_K
\\
V = XW_V
$$

其中
- $W_Q \in \mathbb{R}^{d \times d_q}$
- $W_K \in \mathbb{R}^{d \times d_k}$
- $W_V \in \mathbb{R}^{d \times d_v}$

### 3.3 注意力分数
$$
S = QK^\top
$$

其中 
$$
S_{ij} = q _i ^\top k_j
$$

表示第i个token对第j个token的关注程度。

### 3.4 缩放

$$
\text{Attention}(Q,K,V)=\text{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}\right)V
$$

为什么要除以 
$\sqrt d_k$ 因为当维度$d_k$很大时，点积数值会变大，softmax 会进入饱和区，导致梯度不稳定。 缩放后数值更平稳。

### 3.5 causal mask
$$
\text{Attention}(Q,K,V)=\text{softmax}\left(\frac{QK^\top + M}{\sqrt{d_k}}\right)V
$$

其中 mask \(M\) 满足：

$$
M_{ij}=
\begin{cases}
0, & j \le i \\
-\infty, & j > i
\end{cases}
$$

含义
这样 softmax 后未来位置的权重为 0。

### 3.6 多头注意力
单头注意力只能在一种子空间中建模关系，多头注意力允许模型从不同角度关注信息。


设有 h 个头，第 r个头
$$
Q_r=XW_Q^{(r)}, K_r=XW_K^{(r)}, V_r=XW_V^{(r)}
\\
$$

每个头的输出: 
$$
head_r = Attention(Q_r, K_r, V_r)
$$

然后拼接:
$$
MultiHead(X)= Concat(head1, ..., head_h)W_O
$$

其中:
- $W_O$是输出投影矩阵

不同头可以学习不同模式，例如：
- 语法依赖
- 指代消解
- 长距离关系
- 局部短语关系

## 4. 前馈网络(FFN/MLP)
每个 Transformer block 中，除了 attention，还有逐位置的非线性变换。

经典公式：
$$
FFN(x) = W_2 \sigma (W_1 x + b_1) + b_2
$$

其中:
- $W_1 \in \mathbb {R} ^ {d \times d_{ff}}$
- $W_2 \in \mathbb {R} ^ {d_{ff} \times d}$
- $\sigma$是激活函数, 如ReLU/ GELU/ SwiGLU

### 1. GELU
$$
GELU(x) = x \Phi(x)
$$

常用近似
$$
\text{GELU}(x)\approx 0.5x\left(1+\tanh\left(\sqrt{\frac{2}{\pi}}(x+0.044715x^3)\right)\right)
$$

### 2. SwiGLU
现代 LLM 常用 SwiGLU：
$$
\text{SwiGLU}(x) = (xW_a) \odot \text{Swish}(xW_b)
$$

其中
$$
\text{Swish}(z)=z\sigma(z)
\\
\sigma(z)=\frac{1}{1+e^{-z}}
$$

SwiGLU 相比普通 ReLU/GELU 往往具有更好的表达能力和训练效果。

## 5. 残差连接
每个子层通常带残差:
$$
y = x + Sublayer(x)
$$

含义: 帮助环节深层网络训练中的梯度消失问题

## 6. 归一化
1. LayerNomr
$$
\text{LayerNorm}(x)=\gamma \odot \frac{x-\mu}{\sqrt{\sigma^2+\epsilon}}+\beta
$$
其中:
$$
\mu = \frac{1}{d}\sum_{i=1}^{d}x_i,\quad
\sigma^2 = \frac{1}{d}\sum_{i=1}^{d}(x_i-\mu)^2
$$

2. RMSNorm

很多现代 LLM 使用 RMSNorm：

$$
\text{RMSNorm}(x)=\gamma \odot \frac{x}{\sqrt{\frac{1}{d}\sum x_i^2+\epsilon}}
$$

含义: RMSNorm 不减均值，只按均方根缩放，计算更简单，实践中效果很好。

## 7、Transformer Block 的完整结构

一个典型 GPT-style block 可写成：

Pre-Norm 结构（现代模型常见）

先归一化再计算：

$$
\tilde{x} = x + \text{Attention}(\text{Norm}(x))
\\
y = \tilde{x} + \text{FFN}(\text{Norm}(\tilde{x}))
$$

堆叠 $L$ 层后：

$$
X^{(l+1)} = \text{Block}(X^{(l)}),\quad l=1,\dots,L
$$

## 8. 输出层和概率计算

Transformer 最后一层输出隐藏状态 $h_t$ ​ ，通过线性层映射到词表维度：
$$
z_t=h_tW_{lm} + b
$$

其中
- $z_t \in \mathbb {R} ^{V}$ 是logits
再通过softmax 得到每个token的概率:
$$
P(x_{t+1}=i \mid x_{\le t}) = \frac{e^{z_{t,i}}}{\sum_{j=1}^{V} e^{z_{t,j}}}
$$

